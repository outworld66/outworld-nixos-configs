diff -ruN a/modules/lock/LockContext.qml b/modules/lock/LockContext.qml
--- a/modules/lock/LockContext.qml
+++ b/modules/lock/LockContext.qml
@@ -23,6 +23,9 @@
     property bool fingerprintsConfigured: false
     property var targetAction: LockContext.ActionEnum.Unlock
     property bool alsoInhibitIdle: false
+    // Fingerprint feedback for the lock surface: "ready" | "scanning" | "bad".
+    property string fingerState: "ready"
+    property string fingerMessage: ""
 
     function resetTargetAction() {
         root.targetAction = LockContext.ActionEnum.Unlock;
@@ -87,6 +90,8 @@
         if (!GlobalStates.screenLocked || !root.fingerprintsConfigured || fingerPam.active)
             return;
         fingerprintRetryTimer.stop();
+        root.fingerState = "ready";
+        root.fingerMessage = "";
         if (!fingerPam.start())
             fingerprintRetryTimer.restart();
     }
@@ -129,6 +134,12 @@
     PamContext {
         id: pam
 
+        // Upstream default config is /etc/pam.d/login, whose stack starts
+        // with pam_fprintd: a correct password then waits on the reader
+        // instead of unlocking. Use a dedicated pam_unix-only config.
+        configDirectory: "pam"
+        config: "password.conf"
+
         // pam_unix will ask for a response for the password prompt
         onPamMessage: {
             if (this.responseRequired) {
@@ -158,11 +169,32 @@
         configDirectory: "pam"
         config: "fprintd.conf"
 
+        // pam_fprintd relays reader status through prompts that require a
+        // response; without one the subprocess blocks forever. Reply with an
+        // empty string and surface the status message to the lock surface.
+        onPamMessage: {
+            if (this.responseRequired)
+                this.respond("");
+            if (!this.messageIsError) {
+                root.fingerMessage = this.message;
+                root.fingerState = "scanning";
+            }
+        }
+
+        // A wedged reader or PAM error does not emit completed; retry on the
+        // throttling timer so the prompt recovers without a tight loop.
+        onError: {
+            if (GlobalStates.screenLocked && root.fingerprintsConfigured)
+                fingerprintRetryTimer.restart();
+        }
+
         onCompleted: result => {
             if (result == PamResult.Success) {
                 fingerprintRetryTimer.stop();
                 root.unlocked(root.targetAction);
             } else if (GlobalStates.screenLocked && root.fingerprintsConfigured) {
+                root.fingerState = "bad";
+                root.fingerMessage = "";
                 // Failed scans, PAM errors and max-tries completions are all
                 // recoverable while the lock remains active. Retry through a
                 // timer so a broken device/PAM stack cannot create a tight loop.
diff -ruN a/modules/lock/LockSurface.qml b/modules/lock/LockSurface.qml
--- a/modules/lock/LockSurface.qml
+++ b/modules/lock/LockSurface.qml
@@ -619,6 +619,44 @@
                         onTriggered: dateText.text = Qt.formatDate(new Date(), "dddd, d MMMM")
                     }
                 }
+
+                // Fingerprint availability and scan status on the clock view.
+                Loader {
+                    active: root.context.fingerprintsConfigured
+                    Layout.alignment: clockView.clockPosition === "center" ? Qt.AlignHCenter : Qt.AlignLeft
+
+                    sourceComponent: Row {
+                        spacing: 6
+
+                        MaterialSymbol {
+                            anchors.verticalCenter: parent.verticalCenter
+                            text: "fingerprint"
+                            iconSize: 18
+                            fill: 1
+                            color: root.context.fingerState === "bad"
+                                ? Appearance.colors.colError
+                                : root.context.fingerState === "scanning"
+                                    ? Appearance.colors.colPrimary
+                                    : Appearance.colors.colOnSurfaceVariant
+                        }
+
+                        Text {
+                            anchors.verticalCenter: parent.verticalCenter
+                            text: root.context.fingerState === "bad"
+                                ? Translation.tr("Fingerprint not recognized")
+                                : root.context.fingerState === "scanning" && root.context.fingerMessage !== ""
+                                    ? root.context.fingerMessage
+                                    : Translation.tr("Touch sensor to unlock")
+                            font.pixelSize: Appearance.font.pixelSize.small
+                            font.family: Appearance.font.family.main
+                            color: root.context.fingerState === "bad"
+                                ? Appearance.colors.colError
+                                : root.context.fingerState === "scanning"
+                                    ? Appearance.colors.colPrimary
+                                    : Appearance.colors.colOnSurfaceVariant
+                        }
+                    }
+                }
             }
 
             // Analog clock - CookieClock from background widgets
@@ -1407,7 +1445,11 @@
                             text: "fingerprint"
                             iconSize: 22
                             fill: 1
-                            color: Appearance.colors.colOnSurfaceVariant
+                            color: root.context.fingerState === "bad"
+                                ? Appearance.colors.colError
+                                : root.context.fingerState === "scanning"
+                                    ? Appearance.colors.colPrimary
+                                    : Appearance.colors.colOnSurfaceVariant
                         }
                     }
                     
@@ -1418,6 +1460,7 @@
                         verticalAlignment: Text.AlignVCenter
                         
                         echoMode: TextInput.Password
+                        clip: true
                         inputMethodHints: Qt.ImhSensitiveData
                         font.pixelSize: Appearance.font.pixelSize.large
                         font.family: Appearance.font.family.main
@@ -1468,6 +1511,7 @@
                                 leftMargin: 4
                                 rightMargin: 4
                             }
+                            clip: true
                             sourceComponent: PasswordChars {
                                 length: root.context.currentText.length
                             }
@@ -1574,10 +1618,18 @@
                 visible: active
                 
                 sourceComponent: Text {
-                    text: Translation.tr("Touch sensor to unlock")
+                    text: root.context.fingerState === "bad"
+                        ? Translation.tr("Fingerprint not recognized")
+                        : root.context.fingerState === "scanning" && root.context.fingerMessage !== ""
+                            ? root.context.fingerMessage
+                            : Translation.tr("Touch sensor to unlock")
                     font.pixelSize: Appearance.font.pixelSize.small
                     font.family: Appearance.font.family.main
-                    color: Appearance.colors.colOnSurfaceVariant
+                    color: root.context.fingerState === "bad"
+                        ? Appearance.colors.colError
+                        : root.context.fingerState === "scanning"
+                            ? Appearance.colors.colPrimary
+                            : Appearance.colors.colOnSurfaceVariant
                     
                     layer.enabled: Appearance.effectsEnabled && !root._zzz
                     layer.effect: DropShadow {
